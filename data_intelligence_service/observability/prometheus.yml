global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "zeroque-intelligence"
    static_configs:
      - targets: ["host.docker.internal:8004"]  # app running locally or in docker
    metrics_path: /metrics
